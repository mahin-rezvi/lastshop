# Medimart

Medimart is a Next.js ecommerce storefront with customer auth, account pages,
cart and checkout flows, admin management screens, and Firebase-backed API
routes with local fallback behavior.

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Checks

```bash
npm run lint
npm run build
```
